exp(_exp)  or  exp=_exp;

delete exp;

Program::clear()  /Program::list()
    for  or while

Program::getFirstLineNumber()
    if(allLines.empty()) return -1;

convert()
    INPUT~GOTO
    if(scanner.hasMoreTokens()) throw ErrorException("SYNTAX ERROR");

statement.cpp
     string svalue;
        int value;
        while(true)
        {
            try
            {
                svalue=getLine("?");
                value=stringToInteger(svalue);
            } catch (...)
            {
                cerr<<"INVALID NUMBER"<<endl;
                continue;
            }
            break;
        }
        state.setValue(var,value);

trace 60~64 ?

